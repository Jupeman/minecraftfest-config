minecraftfest-config

Repo layout:
- shared/plugin-jars/   Plugin jars you want deployed to all servers
- shared/config/        Shared config files (paper/spigot/bukkit/etc.)
- servers/<name>/       Per-server overrides (server.properties, etc.)
- scripts/              Deploy scripts (later)
